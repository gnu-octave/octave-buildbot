# -*- python -*-
# ex: set filetype=python:

from buildbot.plugins import *
from buildbot.process.properties import Property

# This is a sample buildmaster config file.  It must be installed as
# 'master.cfg' in your buildmaster's base directory.

# This is the dictionary that the buildmaster pays attention to.  We also use
# a shorter alias to save typing.
c = BuildmasterConfig = {}


####### WORKERS

# The 'workers' list defines the set of recognized workers.  Each element is
# a Worker object, specifying a unique worker name and password.  The same
# worker name and password must be configured on the worker.
c['workers'] = [
  worker.Worker(
    "worker01", "secret_password", max_builds = 1
  )
]
my_workers = []
for w in c['workers']:
  my_workers.append(w.workername)

# 'protocols' contains information about protocols which master will use for
# communicating with workers.  You must define at least 'port' option that
# workers could connect to your master with this protocol.
# 'port' must match the value configured into the workers (with their
# --master option)
c['protocols'] = {
  'pb': {'port': 9989}
}

####### CHANGESOURCES

# The 'change_source' setting tells the buildmaster how it should find out
# about source code changes.

c['change_source'] = []

####### SCHEDULERS

# Configure the Schedulers, which decide how to react to incoming changes.

c['schedulers'] = [
  schedulers.ForceScheduler(
    name = "force",
    builderNames = [
      "octave-stable",
      "octave-mxe-stable-w32",
      "octave-mxe-stable-w64",
      "octave-mxe-stable-w64-64",
      "octave-stable-doxygen"
    ]
  ),
  schedulers.Triggerable(
    name = "trigger",
    builderNames = [
      "octave-mxe-stable-w32",
      "octave-mxe-stable-w64",
      "octave-mxe-stable-w64-64",
      "octave-stable-doxygen"
    ]
  )
]

####### BUILDERS

# The 'builders' list defines the Builders, which tell Buildbot how to perform
# a build: what steps, and which workers can execute them.  Note that any
# particular build will only take place on one worker.

OctaveStable = util.BuildFactory()
OctaveStable.addSteps([
  steps.ShellCommand(
    name = "cleanup build directory",
    command = ["rm", "-Rf", "build"],
    haltOnFailure = True,
    workdir = "."
  ),
  steps.Git(
    name = "clone gnulib repository",
    repourl = "git://git.savannah.gnu.org/gnulib.git",
    mode = "full",
    method = "fresh",
    haltOnFailure = True,
    workdir = "gnulib"
  ),
  steps.Mercurial(
    name = "clone Octave repository",
    repourl = "https://www.octave.org/hg/octave",
    mode = "full",
    method = "fresh",
    branchType = "inrepo",
    defaultBranch = "stable",
    haltOnFailure = True,
    workdir = "src"
  ),
  steps.SetPropertyFromCommand(
    name = "get OCTAVE_HG_ID",
    command = "hg identify --id",
    property = "OCTAVE_HG_ID",
    haltOnFailure = True,
    workdir = "src"
  ),
  steps.SetPropertyFromCommand(
    name = "get OCTAVE_VERSION",
    command = 'grep -e "^AC_INIT" configure.ac | grep -Po "(\d+\.)+\d+"',
    property = "OCTAVE_VERSION",
    haltOnFailure = True,
    workdir = "src"
  ),
  steps.ShellCommand(
    name = "bootstrap",
    command = ["./bootstrap", "--gnulib-srcdir=../gnulib"],
    haltOnFailure = True,
    workdir = "src"
  ),
  steps.ShellCommand(
    name = "configure",
    command = ["../src/configure"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "make",
    command = ["xvfb-run", "make", "-j4"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "make check",
    command = ["make", "check"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "make dist",
    command = ["make", "dist"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "compress documentation",
    command = ["zip", "-r", "interpreter.zip", "octave.html", "octave.pdf"],
    haltOnFailure = True,
    workdir = "build/doc/interpreter"
  ),
  steps.MultipleFileUpload(
    name = "publish build",
    workersrcs = [
      util.Interpolate("octave-%(prop:OCTAVE_VERSION)s.tar.gz"),
      util.Interpolate("octave-%(prop:OCTAVE_VERSION)s.tar.lz"),
      util.Interpolate("octave-%(prop:OCTAVE_VERSION)s.tar.xz"),
      "doc/interpreter/interpreter.zip"
    ],
    masterdest = util.Interpolate("/buildbot/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    url = util.Interpolate("https://www.octave.space/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.MasterShellCommand(
    name = "unzip documentation",
    command = util.Interpolate("cd %(prop:OCTAVE_HG_ID)s && unzip -q interpreter.zip"),
    workdir = "/buildbot/data/stable"
  ),
  steps.MasterShellCommand(
    name = "set file permission on master",
    command = "chmod -R +r .",
    haltOnFailure = True,
    workdir = "/buildbot/data"
  ),
  steps.Trigger(
    schedulerNames = ["trigger"],
    set_properties = {
      "OCTAVE_HG_ID"   : Property("OCTAVE_HG_ID"),
      "OCTAVE_VERSION" : Property("OCTAVE_VERSION")
    }
  )
])


OctaveStableDoxygen = util.BuildFactory()
OctaveStableDoxygen.addSteps([
  steps.ShellCommand(
    name = "cleanup build directory",
    command = ["rm", "-Rf", "build"],
    haltOnFailure = True,
    workdir = "."
  ),
  steps.ShellCommand(
    name = "download Octave release tarball",
    command = ["wget", "-q", util.Interpolate(
      "https://www.octave.space/data/stable/%(prop:OCTAVE_HG_ID)s/octave-%(prop:OCTAVE_VERSION)s.tar.lz")],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "extract Octave release tarball",
    command = [
      "tar", "-xf", util.Interpolate("octave-%(prop:OCTAVE_VERSION)s.tar.lz"),
             "--strip=1"
    ],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "configure",
    command = ["./configure"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "make doxyhtml",
    command = ["make", "doxyhtml"],
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.ShellCommand(
    name = "compress doxyhtml",
    command = ["zip", "-r", "doxyhtml.zip", "doxyhtml",
                      "-x", "octave-cpp-api.qch"],
    haltOnFailure = True,
    workdir = "build/doc"
  ),
  steps.MultipleFileUpload(
    name = "publish documentation",
    workersrcs = [
      "doc/doxyhtml.zip"
    ],
    masterdest = util.Interpolate("/buildbot/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    url = util.Interpolate("https://www.octave.space/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    haltOnFailure = True,
    workdir = "build"
  ),
  steps.MasterShellCommand(
    name = "unzip documentation",
    command = util.Interpolate("cd %(prop:OCTAVE_HG_ID)s && unzip -q doxyhtml.zip"),
    workdir = "/buildbot/data/stable"
  ),
  steps.MasterShellCommand(
    name = "set file permission on master",
    command = "chmod -R +r .",
    workdir = "/buildbot/data"
  )
])


mxe_stable_common_steps_pre = [
  steps.ShellCommand(
    name = "cleanup build directories",
    command = ["rm", "-Rf", "build", "octave"],
    haltOnFailure = True,
    workdir = "."
  ),
  steps.ShellCommand(
    name = "cleanup Octave release tarball",
    command = [
      "rm", "-f", util.Interpolate("octave-%(prop:OCTAVE_VERSION)s.tar.lz")
    ],
    haltOnFailure = True,
    workdir = "../mxe-octave-pkg"
  ),
  steps.ShellCommand(
    name = "download Octave release tarball",
    command = ["wget", "-q", util.Interpolate(
      "https://www.octave.space/data/stable/%(prop:OCTAVE_HG_ID)s/octave-%(prop:OCTAVE_VERSION)s.tar.lz")],
    haltOnFailure = True,
    workdir = "../mxe-octave-pkg"
  ),
  steps.ShellCommand(
    name = "build minimal Octave (1/4)",
    command = [
      "tar", "-xf", util.Interpolate("../../mxe-octave-pkg/octave-%(prop:OCTAVE_VERSION)s.tar.lz")
    ],
    haltOnFailure = True,
    workdir = "octave"
  ),
  steps.ShellCommand(
    name = "build minimal Octave (2/4)",
    command = ["mv", util.Interpolate("octave-%(prop:OCTAVE_VERSION)s"), "build"],
    haltOnFailure = True,
    workdir = "octave"
  ),
  steps.ShellCommand(
    name = "build minimal Octave (3/4)",
    command = ["./configure", "--disable-docs", "--disable-fftw-threads",
      "--disable-java", "--disable-jit", "--disable-openmp",
      "--disable-readline", "--without-amd", "--without-arpack",
      "--without-bz2", "--without-camd", "--without-ccolamd",
      "--without-cholmod", "--without-colamd", "--without-curl",
      "--without-cxsparse", "--without-fftw3", "--without-fftw3f",
      "--without-fltk", "--without-fontconfig", "--without-framework-opengl",
      "--without-freetype", "--without-glpk", "--without-hdf5",
      "--without-klu", "--without-magick", "--without-opengl",
      "--without-openssl", "--without-portaudio", "--without-qhull",
      "--without-qrupdate", "--without-qscintilla", "--without-qt",
      "--without-sndfile", "--without-sundials_ida",
      "--without-sundials_nvecserial", "--without-umfpack", "--without-x",
      "--without-z", "--prefix=$PWD/../install"],
    haltOnFailure = True,
    workdir = "octave/build"
  ),
  steps.ShellCommand(
    name = "build minimal Octave (4/4)",
    command = ["make", "-j4", "install"],
    haltOnFailure = True,
    workdir = "octave/build"
  ),
  steps.ShellCommand(
    name = "mxe-octave repository",
    command = ["hg", "clone", "https://hg.octave.org/mxe-octave", "."],
    haltOnFailure = True
  ),
  steps.ShellCommand(
    name = "bootstrap",
    command = ["./bootstrap"],
    haltOnFailure = True
  )
]


mxe_stable_common_steps_post = [
  steps.ShellCommand(
    name = "make",
    command = ["make", "JOBS=4", "all", "7z-dist", "zip-dist", "nsis-installer"],
    timeout = 3600,
    env={'PATH': "${PWD}/../octave/install/bin:${PATH}"},
    haltOnFailure = True
  ),
  steps.SetPropertyFromCommand(
    name = "get MXE_OCTAVE_ID",
    command = "ls octave-*-installer.exe | grep -Po '(?<=octave-).*(?=-installer.exe)'",
    property = "MXE_OCTAVE_ID",
    haltOnFailure = True,
    workdir = "build/dist"
  ),
  steps.MultipleFileUpload(
    name = "publish build",
    workersrcs = [
      util.Interpolate("octave-%(prop:MXE_OCTAVE_ID)s-installer.exe"),
      util.Interpolate("octave-%(prop:MXE_OCTAVE_ID)s.7z"),
      util.Interpolate("octave-%(prop:MXE_OCTAVE_ID)s.zip")
    ],
    masterdest = util.Interpolate("/buildbot/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    url = util.Interpolate("https://www.octave.space/data/stable/%(prop:OCTAVE_HG_ID)s/"),
    workdir = "build/dist"
  ),
  steps.MasterShellCommand(
    name = "set file permission on master",
    command = "chmod -R +r .",
    workdir = "/buildbot/data"
  )
]


OctaveMxeStableW32 = util.BuildFactory()
OctaveMxeStableW32.addSteps(mxe_stable_common_steps_pre)
OctaveMxeStableW32.addSteps([
  steps.ShellCommand(
    name = "configure",
    command = [
      "./configure",
      "--enable-devel-tools",
      "--enable-binary-packages",
      "--enable-octave=stable",
      "--with-pkg-dir=../../mxe-octave-pkg",
      "--disable-windows-64"
    ],
    haltOnFailure = True
  )
])
OctaveMxeStableW32.addSteps(mxe_stable_common_steps_post)


OctaveMxeStableW64 = util.BuildFactory()
OctaveMxeStableW64.addSteps(mxe_stable_common_steps_pre)
OctaveMxeStableW64.addSteps([
  steps.ShellCommand(
    name = "configure",
    command = [
      "./configure",
      "--enable-devel-tools",
      "--enable-binary-packages",
      "--enable-octave=stable",
      "--with-pkg-dir=../../mxe-octave-pkg"
    ],
    haltOnFailure = True
  )
])
OctaveMxeStableW64.addSteps(mxe_stable_common_steps_post)


OctaveMxeStableW64_64 = util.BuildFactory()
OctaveMxeStableW64_64.addSteps(mxe_stable_common_steps_pre)
OctaveMxeStableW64_64.addSteps([
  steps.ShellCommand(
    name = "configure",
    command = [
      "./configure",
      "--enable-devel-tools",
      "--enable-binary-packages",
      "--enable-octave=stable",
      "--with-pkg-dir=../../mxe-octave-pkg",
      "--enable-fortran-int64"
    ],
    haltOnFailure = True
  )
])
OctaveMxeStableW64_64.addSteps(mxe_stable_common_steps_post)


c['builders'] = [
  util.BuilderConfig(
    name = "octave-stable",
    workernames = my_workers,
    factory = OctaveStable
  ),
  util.BuilderConfig(
    name = "octave-stable-doxygen",
    workernames = my_workers,
    factory = OctaveStableDoxygen
  ),
  util.BuilderConfig(
    name = "octave-mxe-stable-w32",
    workernames = my_workers,
    factory = OctaveMxeStableW32
  ),
  util.BuilderConfig(
    name = "octave-mxe-stable-w64",
    workernames = my_workers,
    factory = OctaveMxeStableW64
  ),
  util.BuilderConfig(
    name = "octave-mxe-stable-w64-64",
    workernames = my_workers,
    factory = OctaveMxeStableW64_64
  )
]

####### BUILDBOT SERVICES

# 'services' is a list of BuildbotService items like reporter targets. The
# status of each build will be pushed to these targets. buildbot/reporters/*.py
# has a variety to choose from, like IRC bots.

c['services'] = []

####### PROJECT IDENTITY

# the 'title' string will appear at the top of this buildbot installation's
# home pages (linked to the 'titleURL').

c['title'] = "Octave Buildbot Master [unconfigured]"
c['titleURL'] = "https://www.octave.org/"

# the 'buildbotURL' string should point to the location where the buildbot's
# internal web server is visible. This typically uses the port number set in
# the 'www' entry below, but with an externally-visible host name which the
# buildbot cannot figure out without some help.

c['buildbotURL'] = "http://localhost:8010/"

# Tuples of usernames and passwords for administrators authorized to login
# to the web interface and trigger builds, shutdown workers, etc.

my_admins = {"user01": "secret_password"}
my_admin_names = my_admins.keys()

# minimalistic config to activate new web UI
c['www'] = {
  "port": 8010,
  "auth": util.UserPasswordAuth(my_admins),
  "authz": util.Authz(
    allowRules = [
      util.AnyControlEndpointMatcher(role = "admins")
    ],
    roleMatchers = [
      util.RolesFromUsername(roles = ["admins"],
                             usernames = my_admin_names)
    ]
  ),
  "plugins": {
    "waterfall_view": {},
    "console_view": {},
    "grid_view": {}
    }
}

####### DB URL

c['db'] = {
  # This specifies what database buildbot uses to store its state.
  # It's easy to start with sqlite, but it's recommended to switch to a
  # dedicated database, such as PostgreSQL or MySQL, for use in production
  # environments.
  # http://docs.buildbot.net/current/manual/configuration/global.html#database-specification
  'db_url' : "sqlite:///state.sqlite",
}
